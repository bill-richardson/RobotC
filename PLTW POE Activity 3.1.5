#pragma config(StandardModel, "POE + CIM Testbed")
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

/*
Project Title:
Team Members:
Date:
Section:


Task Description:b.	Modify the program so that a variable "brightest" will
remember the brightest light detected by the light sensor.
The limit switch should reset the record.


Pseudocode:

*/
int brightest = 1000;                   // declare and set integer brightest to 0
int currentlight = 0;                 // declare variable to watch in debugger to confirm working
task main()
{                                      //Program begins, insert code within curly braces


	while (true)                         // infinite loop
	{
		if (brightest >SensorValue[lightSensor])   // if variable is greater than the light sensor reading
		{
			brightest=SensorValue[lightSensor];      // replace brightest with reading from sonar
			currentlight = SensorValue[lightSensor];  // variable used to display current light sensor reading
		}
		if (SensorValue[limitSwitch]==1)   // if limit switch is pressed
    {
      brightest = 1000;                 // reset variable brightest to 1000
    }

	}



}
